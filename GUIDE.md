# 表单设计器使用指南

## 快速演示步骤

### 1. 启动项目

```bash
# 安装依赖
pnpm install

# 启动开发服务器
pnpm dev
```

访问 http://localhost:5173（或终端显示的地址）

### 2. 界面说明

项目启动后，你会看到三个主要区域：

- **左侧面板**：基础字段库，包含各种表单组件
- **中间区域**：表单设计区，显示表单标题、描述和字段
- **右侧面板**：配置面板，点击字段后显示，可配置字段属性

### 3. 演示操作流程

#### 步骤 1：查看初始表单
项目默认已经创建了一个"用户信息表"，包含以下字段：
- 姓名（单行文本）
- 邮箱（单行文本）
- 个人简介（多行文本）
- 性别（单选框）
- 兴趣爱好（多选框）

#### 步骤 2：编辑表单信息
1. 点击表单标题"用户信息表"，可以修改表单名称
2. 点击"请填写您的个人信息"，可以修改表单描述
3. 修改完成后点击页面其他位置保存

#### 步骤 3：添加新字段
从左侧面板拖拽任意组件到表单设计区：
- **单行文本**：适合姓名、邮箱等短文本输入
- **多行文本**：适合备注、描述等长文本输入
- **计数器**：适合年龄、数量等数字输入
- **下拉选择**：适合单选项目选择
- **日期选择**：适合日期时间选择
- **颜色选择**：适合颜色选择
- **单选框**：适合单选题
- **多选框**：适合多选题

#### 步骤 4：配置字段属性
1. 点击表单中的任意字段
2. 右侧会出现配置面板
3. 可以修改：
   - **字段标题**：显示的标签文字
   - **字段宽度**：25%、50%、75%、100%
   - **组件类型**：显示当前组件类型（不可修改）
4. 点击"删除字段"可以移除该字段

#### 步骤 5：调整字段顺序
直接在表单设计区拖拽字段，可以调整字段的显示顺序

#### 步骤 6：预览表单
1. 点击右上角的"预览"按钮
2. 查看表单的实际显示效果
3. 点击遮罩层外部区域关闭预览

### 4. 演示场景示例

#### 场景 1：创建用户注册表单
1. 修改表单名为"用户注册"
2. 添加字段：
   - 用户名（单行文本）
   - 密码（单行文本）
   - 确认密码（单行文本）
   - 性别（单选框）
   - 出生日期（日期选择）
   - 兴趣标签（多选框）

#### 场景 2：创建问卷调查表
1. 修改表单名为"产品满意度调查"
2. 添加字段：
   - 您的姓名（单行文本）
   - 满意度评分（计数器）
   - 使用频率（下拉选择）
   - 最喜欢的功能（多选框）
   - 改进建议（多行文本）

#### 场景 3：创建活动报名表
1. 修改表单名为"技术沙龙报名"
2. 添加字段：
   - 姓名（单行文本）
   - 公司（单行文本）
   - 职位（单行文本）
   - 联系电话（单行文本）
   - 参加日期（日期选择）
   - 感兴趣的话题（多选框）

### 5. 注意事项

- 首次访问会有操作引导（intro.js），可以跟随引导了解功能
- 表单数据暂存在浏览器内存中，刷新页面会重置
- 目前不支持表单数据的保存和导出（功能待开发）
- 拖拽时请确保鼠标在组件区域内释放

### 6. 键盘快捷键

目前支持的交互：
- **Enter 键**：编辑表单信息时按 Enter 保存
- **ESC 键**：关闭预览模式（待实现）

### 7. 常见问题

**Q: 如何删除字段？**
A: 点击字段后，在右侧配置面板点击"删除字段"按钮

**Q: 字段能否复制？**
A: 目前不支持，可以从左侧重新拖拽添加

**Q: 表单数据如何保存？**
A: 当前版本暂不支持保存功能，仅供演示使用

**Q: 能否自定义组件？**
A: 可以在 `src/components/plugins/` 目录下添加新的 Vue 组件

### 8. 开发说明

如需扩展功能，主要文件：
- `src/store/index.ts` - 状态管理
- `src/components/plugins/` - 表单组件
- `src/components/plugins/publicPlugins.ts` - 组件配置
- `src/components/PluginConfig.vue` - 配置面板

祝使用愉快！🎉
