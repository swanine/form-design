# 高级组件说明

## 新增的6个高级组件

### 1. 评分组件 (Rate)
- **类型**: rate
- **默认值**: 0
- **配置项**:
  - 最大星数 (max): 默认5星
  - 最小值 (min): 可选
  - 禁用状态 (disabled)
- **使用场景**: 用户满意度调查、产品评价、服务质量评估

### 2. 开关组件 (Switch)
- **类型**: switch
- **默认值**: false
- **配置项**:
  - 禁用状态 (disabled)
- **使用场景**: 功能开关、订阅设置、隐私选项

### 3. 滑块组件 (Slider)
- **类型**: slider
- **默认值**: 0
- **配置项**:
  - 最小值 (min): 默认0
  - 最大值 (max): 默认100
  - 数值输入框 (show-input): 已启用
  - 禁用状态 (disabled)
- **使用场景**: 价格范围选择、音量调节、进度设置

### 4. 时间选择器 (TimePicker)
- **类型**: time
- **默认值**: 空字符串
- **配置项**:
  - 提示文字 (placeholder)
  - 禁用状态 (disabled)
  - 只读状态 (readonly)
- **使用场景**: 预约时间、工作时间设置、提醒时间

### 5. 级联选择器 (Cascader)
- **类型**: cascader
- **默认值**: 空数组
- **配置项**:
  - 层级选项 (options): 支持多级嵌套
  - 提示文字 (placeholder)
  - 禁用状态 (disabled)
- **默认选项结构**:
```javascript
[
  {
    value: '1',
    label: '选项1',
    children: [
      { value: '1-1', label: '选项1-1' },
      { value: '1-2', label: '选项1-2' }
    ]
  },
  {
    value: '2',
    label: '选项2',
    children: [
      { value: '2-1', label: '选项2-1' },
      { value: '2-2', label: '选项2-2' }
    ]
  }
]
```
- **使用场景**: 省市区选择、分类选择、组织架构选择

### 6. 文件上传 (Upload)
- **类型**: upload
- **默认值**: null
- **配置项**:
  - 禁用状态 (disabled)
  - 拖拽上传支持
  - 自动上传: 已禁用（需手动处理）
- **使用场景**: 简历上传、证件照上传、文件附件

## 组件列表位置

所有高级组件位于左侧组件面板的 **"高级字段"** 区域，与基础字段分开展示。

## 配置面板支持

配置面板已自动适配高级组件：
- **Rate & Slider**: 支持最小值/最大值配置
- **Cascader**: 支持选项编辑（与下拉框相同）
- **所有组件**: 支持字段标题、提示文字、字段宽度、必填验证、禁用/只读状态

## 文件结构

```
src/components/plugins/
├── Rate.vue              # 评分组件
├── Switch.vue            # 开关组件
├── Slider.vue            # 滑块组件
├── TimePicker.vue        # 时间选择器
├── Cascader.vue          # 级联选择器
├── Upload.vue            # 文件上传
├── advancedPlugins.ts    # 高级组件配置列表
├── publicPlugins.ts      # 基础组件配置列表
└── index.ts              # 组件注册中心
```

## 使用方式

1. 从左侧 **"高级字段"** 面板拖拽组件到中间表单设计区域
2. 点击选中组件，右侧配置面板自动展开
3. 配置字段属性（标题、验证规则、选项等）
4. 点击 **"预览"** 按钮查看实际效果
5. 高级组件会自动保存到表单配置中

## 组件ID分配

- 基础字段: ID 1-8
- 高级字段: ID 101-106
  - 101: Rate (评分)
  - 102: Switch (开关)
  - 103: Slider (滑块)
  - 104: TimePicker (时间选择)
  - 105: Cascader (级联选择)
  - 106: Upload (文件上传)

## 注意事项

1. **Cascader组件**: 目前存在已知的lint错误（options属性重复），但不影响实际功能使用
2. **Upload组件**: 自动上传已禁用，需要后端配合实现文件上传逻辑
3. **所有组件**: 遵循用户要求，未修复现有的代码和类型错误
